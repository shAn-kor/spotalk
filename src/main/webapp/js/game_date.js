/*document.addEventListener('DOMContentLoaded', function() { //드롭메뉴가 완전히 로드된 후 이벤트
    var menuBox = document.querySelector(".drop_btn");
    var down = document.querySelector(".dropdown-content");

    down.addEventListener('click', function(event) {
        if(event.target.tagName != "A") return;
        menuBox.innerHTML=event.target.innerHTML;
        
    });
});
*/